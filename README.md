# HW3

Database name: blog
table:
1. Users
   username varchar(32),
   password varchar(32),
   firstname varchar(32),
   lastname varchar(32),
   country varchar(32)


2. Posts:
(title varchar(32),
create_date date,
content text)
